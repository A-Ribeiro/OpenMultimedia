if(AVFORMAT_INCLUDE_DIR AND AVFORMAT_LIBRARIES)
	unset(AVFORMAT_INCLUDE_DIR)
	unset(AVFORMAT_LIBRARIES)
endif()

if (AVFORMAT_ROOT)
	find_path(AVFORMAT_INCLUDE_DIR libavformat/avformat.h PATHS ${AVFORMAT_ROOT})
	find_library(AVFORMAT_LIBRARIES NAMES avformat avformat.58 PATHS ${AVFORMAT_ROOT})
else()
	find_path(AVFORMAT_INCLUDE_DIR libavformat/avformat.h)
	find_library(AVFORMAT_LIBRARIES NAMES avformat avformat.58)
endif()

if(AVFORMAT_INCLUDE_DIR AND AVFORMAT_LIBRARIES)
	set(AVFORMAT_FOUND ON)
endif()

if(AVFORMAT_FOUND)
	if (NOT ${CMAKE_FIND_PACKAGE_NAME}_FIND_QUIETLY)
		MESSAGE(STATUS "Found ${CMAKE_FIND_PACKAGE_NAME} include:  ${AVFORMAT_INCLUDE_DIR}")
		MESSAGE(STATUS "Found ${CMAKE_FIND_PACKAGE_NAME} library: ${AVFORMAT_LIBRARIES}")
	endif()
else()
	if(${CMAKE_FIND_PACKAGE_NAME}_FIND_REQUIRED)
		MESSAGE(FATAL_ERROR "Could NOT find ${CMAKE_FIND_PACKAGE_NAME} development files")
	endif()
endif()